# Не используется

Whisper больше не хранится в проекте. Модель и бинарник живут в постоянной папке
вне проекта, чтобы обновление проекта их не удаляло:

```
%LOCALAPPDATA%\Terra\whisper
```

`Start-Terra.bat` скачивает их автоматически при первом запуске.
Подробности — `docs/WHISPER.md`.
